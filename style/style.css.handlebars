html, .view body { background-color: black; counter-reset: slideidx; }
body, .view section { background-color: white; border-radius: 12px }
/* A section is a slide. It's size is 800x600, and this will never change */
section, .view head > title {
  /* The font from Google */
  font-family: 'Roboto', serif;
  font-weight: thin;
  font-size: {{x_size 30}}px;
}

section > div {
  margin-left: {{x_size 75}}px;
}

.view section:after {
  counter-increment: slideidx;
  content: counter(slideidx, decimal-leading-zero);
  position: absolute; bottom: -{{y_size 80}}px; right: {{x_size 100}}px;
  color: white;
}

.view head > title {
  color: white;
  text-align: center;
  margin: 1em 0 1em 0;
}

h1 {
  margin-top: {{y_size 200}}px;
  text-align: center;
  font-size: {{font_size 80}}px;
}
h2 {
  margin-top: {{y_size 100}}px;
  text-align: center;
  font-size: {{font_size 80}}px;
}
h3 {
  margin: {{y_size 100}}px 0 {{y_size 50}}px {{x_size 100}}px;
}

ul {
    margin: {{y_size 50}}px {{x_size 80}}px;
}
li > ul {
  margin: {{y_size 15}}px {{x_size 50}}px;
}
li {
  margin-bottom: {{y_size 20}}px;
}

ol {
    margin: {{y_size 50}}px {{x_size 80}}px;
}
li > ol {
  margin: {{y_size 15}}px {{x_size 50}}px;
}

p {
  margin: {{x_size 75}}px;
  font-size: {{font_size 50}}px;
}

blockquote {
  height: 100%;
  background-color: black;
  color: white;
  font-size: {{font_size 60}}px;
  padding: {{x_size 50}}px;
}
blockquote:before {
  content: open-quote;
}
blockquote:after {
  content: close-quote;
}
cite {
  background-color: black;
  display: block;
  width: {{x_size 700}}px;
  text-align: right;
  color: white;
  font-size: {{font_size 15}}px;
  position: relative;
  top: -{{y_size 80}}px;
  margin-right: 50px;
}

/* Figures are displayed full-page, with the caption
   on top of the image/video */
   /*
figure {
  background-color: black;
  width: 100%;
  height: 100%;
}
figure > * {
  position: absolute;
}
figure > img, figure > video {
  width: 100%; height: 100%;
}
figcaption {
  margin: {{x_size 70}}px;
  font-size: {{font_size 50}}px;
}*/

.images > h3 {
  margin: {{y_size 40}}px 0 {{y_size 30}}px {{x_size 100}}px;
}

.images table {
  margin-left: {{x_size 40}}px;
}

.images td {
  vertical-align: bottom;
  text-align: center;
  width: {{x_size 350}}px;
  height: {{y_size 230}}px;
}

a {
  color: #222;
}

footer > a {
  color: #ddd;
}

footer {
  position: absolute;
  bottom: 0;
  width: 100%;
  padding: {{y_size 5}}px;
  text-align: right;
  background-color: #444;
  font-size: {{font_size 20}}px;
  color:#eee;
}

/* Transition effect */
/* Feel free to change the transition effect for original
   animations. See here:
   https://developer.mozilla.org/en/CSS/CSS_transitions
   How to use CSS3 Transitions: */
/*section {
  -moz-transition: left 400ms linear 0s;
  -webkit-transition: left 400ms linear 0s;
  -ms-transition: left 400ms linear 0s;
  transition: left 400ms linear 0s;
}*/
.view section {
  -moz-transition: none;
  -webkit-transition: none;
  -ms-transition: none;
  transition: none;
}

.view section[aria-selected] {
  border: {{x_size 5}}px red solid;
}

/* Before */
section { left: -150%; }
/* Now */
section[aria-selected] { left: 0; }
/* After */
section[aria-selected] ~ section { left: +150%; }

/* Incremental elements */

/* By default, visible */
.incremental > * { opacity: 1; }

/* The current item */
.incremental > *[aria-selected] { opacity: 1; }

/* The items to-be-selected */
.incremental > *[aria-selected] ~ * { opacity: 0; }

/* The progressbar, at the bottom of the slides, show the global
   progress of the presentation. */
#progress-bar {
  height: {{y_size 2}}px;
  background: #AAA;
}
